{
  "name": "reachout",
  "version": "1.0.0",
  "description": "ReachOut — Vercel deployment manifest. Serves index.html (static, CDN) plus the Express API as a serverless function via api/index.js. Dependencies mirror server/package.json so Vercel installs them at the repo root; keep the two in sync.",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "express": "^4.19.2",
    "firebase-admin": "^13.2.0",
    "pdf-parse": "^2.4.5"
  }
}
